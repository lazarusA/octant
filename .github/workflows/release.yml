name: Release

on:
  push:
    tags:
      - 'v*'

permissions:
  contents: write

jobs:
  publish-crates-io:
    name: Publish to crates.io
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@stable

      - name: Install system dependencies (Linux)
        run: |
          sudo apt-get update
          sudo apt-get install -y libnetcdf-dev libhdf5-dev

      - name: Cache dependencies
        uses: Swatinem/rust-cache@v2

      - name: Publish to crates.io
        env:
          CARGO_REGISTRY_TOKEN: ${{ secrets.CARGO_REGISTRY_TOKEN }}
        run: cargo publish --token "$CARGO_REGISTRY_TOKEN"

  github-release:
    name: Create GitHub Release
    runs-on: ubuntu-latest
    needs: [publish-crates-io]
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          generate_release_notes: true

# FUTURE STEP: Pre-compiled App Bundles (.dmg for macOS, .zip for Windows, .tar.gz for Linux)
# Uncomment the block below once cargo-bundle metadata is configured in Cargo.toml
#
#  build-bundles:
#    name: Build Application Bundles (${{ matrix.target }})
#    continue-on-error: true
#    strategy:
#      fail-fast: false
#      matrix:
#        include:
#          - os: ubuntu-latest
#            target: x86_64-unknown-linux-gnu
#            asset_name: octant-linux-x86_64.tar.gz
#            build_cmd: cargo build --release --target x86_64-unknown-linux-gnu
#            package_cmd: tar -czf octant-linux-x86_64.tar.gz -C target/x86_64-unknown-linux-gnu/release octant
#
#          - os: macos-latest
#            target: aarch64-apple-darwin
#            asset_name: octant-macos-arm64-dmg.tar.gz
#            build_cmd: cargo install cargo-bundle && cargo bundle --release --target aarch64-apple-darwin
#            package_cmd: tar -czf octant-macos-arm64-dmg.tar.gz -C target/aarch64-apple-darwin/release/bundle/osx Octant.dmg
#
#          - os: macos-13
#            target: x86_64-apple-darwin
#            asset_name: octant-macos-x86_64-dmg.tar.gz
#            build_cmd: cargo install cargo-bundle && cargo bundle --release --target x86_64-apple-darwin
#            package_cmd: tar -czf octant-macos-x86_64-dmg.tar.gz -C target/x86_64-apple-darwin/release/bundle/osx Octant.dmg
#
#          - os: windows-latest
#            target: x86_64-pc-windows-msvc
#            asset_name: octant-windows-x86_64.zip
#            build_cmd: cargo build --release --target x86_64-pc-windows-msvc
#            package_cmd: 7z a octant-windows-x86_64.zip target/x86_64-pc-windows-msvc/release/octant.exe
#    runs-on: ${{ matrix.os }}
#    steps:
#      - name: Checkout repository
#        uses: actions/checkout@v4
#
#      - name: Install Rust toolchain
#        uses: dtolnay/rust-toolchain@stable
#        with:
#          targets: ${{ matrix.target }}
#
#      - name: Cache dependencies
#        uses: Swatinem/rust-cache@v2
#
#      - name: Build bundle / executable
#        run: ${{ matrix.build_cmd }}
#
#      - name: Package installer asset
#        run: ${{ matrix.package_cmd }}
#
#      - name: Upload artifact
#        uses: actions/upload-artifact@v4
#        with:
#          name: ${{ matrix.asset_name }}
#          path: ${{ matrix.asset_name }}
