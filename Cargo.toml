[package]
name = "octant"
version = "0.2.6"
default-run = "octant"
authors = ["Lazaro Alonso <lazarus.alon@gmail.com>"]
edition = "2024"
description = "An interactive viewer for n-dimensional datasets with native support for local and cloud object storage, Zarr, and Icechunk. Built in Rust with GPU-accelerated rendering."
readme = "README.md"
repository = "https://github.com/lazarusA/octant"
documentation = "https://docs.rs/octant"
license = "MIT OR Apache-2.0"
keywords = ["visualization", "zarr", "icechunk", "wgpu", "graphics"]
categories = ["visualization", "graphics", "science"]
exclude = [
    ".agents/",
    ".github/",
    "signatures/",
    "assets/octant_tas.png",
    "assets/coastlines/coastline_50m.bin",
    "assets/coastlines/coastline_10m.bin",
    "AGENTS.md",
    "ARCHITECTURE.md",
]

[dependencies]
egui = "0.36"
eframe = { version = "0.36", features = ["wgpu"] }
wgpu = { version = "30", features = ["webgpu", "webgl"] }
bytemuck = { version = "1.25", features = ["derive"] }
glam = { version = "0.33", features = ["bytemuck"] }
log = "0.4"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
image = { version = "0.25", default-features = false, features = ["png", "jpeg", "webp"] }
flate2 = "1.0"
inventory = "0.3"
futures = { version = "0.3", default-features = false, features = ["alloc"] }
web-time = "1.1"
blusc = "0.0.6"
ruzstd = "0.8"
zarrs_metadata = "0.7"
zarrs_codec = "0.2"
zarrs_plugin = "0.4"
bytes = "1.12"
async-trait = "0.1"
half = "2.4"
weezl = "0.2"

icechunk-format = { version = "2.1.2", default-features = false }
icechunk-types = { version = "2.1.2", default-features = false }

[target.'cfg(not(target_arch = "wasm32"))'.dependencies]
tokio = { version = "1.53", features = ["full"] }
env_logger = "0.11"
arboard = "3.4"
netcdf = { version = "0.12", features = ["static"] }
rayon = "1.10"
reqwest = { version = "0.13", features = ["blocking", "json"] }
zarrs = { version = "0.23", features = ["blosc", "zstd", "sharding", "gzip", "zlib", "crc32c", "async", "filesystem"] }
zarrs_object_store = { version = "0.7", features = ["http"] }
zarrs_icechunk = "0.5"
icechunk = "2.1.2"
object_store = { version = "0.14", features = ["http", "aws", "azure", "gcp"] }
async-tiff = { path = "dev/async-tiff", features = ["object_store", "reqwest", "tokio"] }

[target.'cfg(target_arch = "wasm32")'.dependencies]
wasm-bindgen = "0.2"
wasm-bindgen-futures = "0.4"
console_error_panic_hook = "0.1"
console_log = "1.1"
uuid = { version = "1", features = ["v4", "js"] }
zarrs = { version = "0.23", default-features = false, features = ["ndarray", "crc32c", "gzip", "zlib", "sharding", "transpose", "async", "bitround", "pcodec"] }
web-sys = { version = "0.3", features = ["Window", "Document", "Element", "HtmlCanvasElement", "Response", "Request", "RequestInit", "Headers", "RequestMode"] }
js-sys = "0.3"
blusc = "0.0.6"
getrandom = { version = "0.4", features = ["wasm_js"] }
async-tiff = { path = "dev/async-tiff", default-features = false }

[dev-dependencies]
blusc = "0.0.6"

[patch.crates-io]
blusc = { git = "https://github.com/lazarusA/blusc", branch = "la/fix_linker" }
